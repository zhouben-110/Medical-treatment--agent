# 医疗Agent设计文档

## 概述

搭建一个个人健康助手Agent，用户描述症状后，AI通过多轮对话分析症状，最终给出治疗建议。

## 技术栈

- **前端**: Next.js
- **后端**: FastAPI
- **AI框架**: LangChain + LangGraph
- **数据库**: SQLite (MVP) / PostgreSQL (生产)

## 架构设计

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端层 (Next.js)                      │
│         聊天界面 │ 历史记录 │ 症状分类                    │
└─────────────────────────────────────────────────────────┘
                          ↕ WebSocket / REST API
┌─────────────────────────────────────────────────────────┐
│                    API层 (FastAPI)                       │
│           /chat │ /history │ /symptoms                  │
└─────────────────────────────────────────────────────────┘
                          ↕
┌─────────────────────────────────────────────────────────┐
│                   核心层 (LangGraph)                     │
│      症状分析 │ 追问决策 │ 疾病匹配 │ 建议输出            │
└─────────────────────────────────────────────────────────┘
                          ↕
┌─────────────────────────────────────────────────────────┐
│                      数据层                              │
│            SQLite/PostgreSQL │ 对话历史                  │
└─────────────────────────────────────────────────────────┘
```

### LangGraph状态机

**流程:**
```
开始 → 接收症状 → 症状分析 → 信息完整?
                              ↓ 否      ↓ 是
                         追问节点    疾病匹配
                              ↓ 循环      ↓
                                   生成建议 → 结束
```

**状态定义:**
```python
{
    messages: List[Message]        # 对话历史
    symptoms: List[str]            # 提取的症状列表
    current_stage: str             # 当前阶段
    confidence: float              # 诊断置信度
    possible_diseases: List[str]   # 可能的疾病
    treatment_plan: str            # 治疗建议
    need_more_info: bool           # 是否需要更多信息
}
```

**核心节点:**
1. **症状分析节点** — 从用户描述中提取症状，识别关键信息
2. **追问决策节点** — 判断信息是否完整，决定追问内容
3. **疾病匹配节点** — 根据症状组合匹配可能的疾病
4. **建议输出节点** — 生成治疗建议和注意事项

## API接口设计

### POST /api/chat

发送消息，获取AI回复。

**请求:**
```json
{
    "message": "我最近总是头痛",
    "session_id": "optional-session-id"
}
```

**响应:**
```json
{
    "reply": "了解了。请问头痛持续多久了？",
    "stage": "questioning",
    "symptoms": ["头痛"]
}
```

### GET /api/history

获取用户历史对话列表。

**响应:**
```json
{
    "sessions": [
        {
            "id": "session-123",
            "title": "头痛发烧咨询",
            "created_at": "2026-05-24T10:00:00Z",
            "message_count": 6
        }
    ]
}
```

### GET /api/history/{session_id}

获取指定会话的详细记录。

**响应:**
```json
{
    "messages": [
        {
            "role": "user",
            "content": "我最近总是头痛",
            "timestamp": "2026-05-24T10:00:00Z"
        }
    ],
    "diagnosis": "紧张性头痛"
}
```

### GET /api/symptoms

获取症状分类列表。

**响应:**
```json
{
    "categories": [
        {
            "name": "头部",
            "symptoms": ["头痛", "头晕", "耳鸣"]
        }
    ]
}
```

## 数据模型

```sql
-- 用户表
CREATE TABLE users (
    id TEXT PRIMARY KEY,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 会话表
CREATE TABLE sessions (
    id TEXT PRIMARY KEY,
    user_id TEXT REFERENCES users(id),
    title TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    diagnosis TEXT
);

-- 消息表
CREATE TABLE messages (
    id TEXT PRIMARY KEY,
    session_id TEXT REFERENCES sessions(id),
    role TEXT CHECK(role IN ('user', 'assistant')),
    content TEXT,
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 症状分类表（预置数据）
CREATE TABLE symptom_categories (
    id TEXT PRIMARY KEY,
    name TEXT
);

-- 症状表（预置数据）
CREATE TABLE symptoms (
    id TEXT PRIMARY KEY,
    category_id TEXT REFERENCES symptom_categories(id),
    name TEXT
);
```

## 前端界面

### 页面布局

- **左侧边栏**: 新对话按钮 + 历史记录列表
- **右侧主区**: 消息展示区 + 输入框

### 关键交互

1. **流式输出** — AI回复逐字显示，提升用户体验
2. **症状高亮** — 识别出的症状用标签高亮显示
3. **加载状态** — 分析中显示loading动画

## 错误处理

| 场景 | 处理方式 |
|------|----------|
| LLM调用失败 | 降级为预设回复，提示用户稍后重试 |
| 症状无法识别 | 引导用户重新描述，或建议就医 |
| 数据库连接失败 | 内存缓存兜底，异步重试写入 |

**免责声明**: 每次回复末尾附带"仅供参考，不构成医疗建议"

## 测试策略

- **单元测试** — 各节点逻辑独立测试
- **集成测试** — 完整对话流程测试
- **API测试** — 接口请求响应验证
- **E2E测试** — 前端到后端完整流程

## MVP范围

### 核心功能
- 症状描述 → 治疗建议（单轮）
- 多轮对话追问
- 历史记录保存
- 症状分类浏览

### 后续迭代
- ~~RAG接入专业医学数据~~ ✅ 已完成
- 更复杂的诊断逻辑
- 用户账户系统

---

## RAG 医学知识检索增强（v2）

### 设计目标

将疾病匹配和治疗建议从纯 LLM 推理升级为基于医学知识库的检索增强生成，提升诊断准确性、减少幻觉、支持溯源。

### 架构设计

```
┌─────────────────────────────────────────────────────────┐
│                    RAG 检索层                            │
│  ┌──────────────────┐  ┌────────────────────────────┐  │
│  │ 结构化知识库       │  │ 向量检索                    │  │
│  │ knowledge_base.py │  │ Chroma + DashScope 嵌入    │  │
│  │ 25种疾病映射表     │  │ data/guidelines/*.md       │  │
│  └────────┬─────────┘  └─────────────┬──────────────┘  │
│           └──────────┬───────────────┘                  │
│                      ↓                                  │
│            MedicalRetriever                             │
│          retrieve_for_diagnosis()                       │
│          retrieve_for_advice()                          │
└─────────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────────────┐
│                   LangGraph 节点层                       │
│  diagnose: LLM + medical_context → 疾病匹配             │
│  advise:   LLM + medical_context → 治疗建议             │
└─────────────────────────────────────────────────────────┘
```

### 模块设计

#### 1. 结构化知识库 (`app/rag/knowledge_base.py`)

```python
DISEASE_KNOWLEDGE = [
    {
        "disease": "普通感冒",
        "symptoms": ["发热", "咳嗽", "流涕", "鼻塞", "头痛", "乏力"],
        "description": "...",
        "treatment": "...",
        "when_to_see_doctor": "...",
        "severity": "轻"
    },
    # ... 25种疾病
]
```

检索算法：
- `search_by_symptoms(symptoms)`：计算用户症状与每种疾病症状的交集，按 `匹配数/疾病症状总数` 排序，返回 Top 5
- `search_by_disease(disease_name)`：按疾病名精确查找，支持部分匹配

#### 2. 嵌入模型 (`app/rag/embeddings.py`)

使用阿里 DashScope `text-embedding-v3` 模型（1024维），通过 `dashscope.TextEmbedding` 直接调用。

关键约束：单次批量最多 10 条文本，`vector_store.py` 按 `BATCH_SIZE=10` 分批处理。

#### 3. 向量库 (`app/rag/vector_store.py`)

- **存储**：Chroma 本地文件持久化（`chroma_db/`）
- **文档加载**：扫描 `data/guidelines/*.md`，`RecursiveCharacterTextSplitter` 按 500 字/50 字重叠分 chunk
- **初始化策略**：持久化目录存在且集合非空 → 跳过加载；否则从文档创建
- **集合名**：`medical_guidelines`

#### 4. 统一检索接口 (`app/rag/retriever.py`)

```python
class MedicalRetriever:
    async def retrieve_for_diagnosis(symptoms: list[str]) -> str
    # 输出格式：
    # 【知识库匹配结果】
    # 1. 普通感冒（匹配度43%，严重程度：轻）
    #    匹配症状：头痛、发热、咳嗽
    #    描述：由病毒引起的上呼吸道感染...
    # 【医学文献参考】
    # 1. 普通感冒诊疗指南...（向量检索 Top 3）

    async def retrieve_for_advice(diseases: list[str], symptoms: list[str]) -> str
    # 输出格式：
    # 【普通感冒】
    # 描述：...
    # 治疗建议：...
    # 就医指征：...
    # 【普通感冒相关文献】
    # 1. 治疗原则：对症治疗为主...
```

#### 5. 节点改造

**diagnose 节点** (`nodes/disease_matcher.py`)：
- 新增 `retriever` 模块级变量（由 main.py lifespan 注入）
- `match_diseases()` 中先调用 `retriever.retrieve_for_diagnosis(symptoms)`
- Prompt 新增 `医学知识参考: {medical_context}` 段落
- 返回值新增 `retrieved_context` 字段

**advise 节点** (`nodes/advisor.py`)：
- 同样注入 retriever
- `generate_advice()` 中调用 `retriever.retrieve_for_advice(diseases, symptoms)`
- Prompt 新增 `医学知识参考: {medical_context}` 段落

#### 6. State 扩展

```python
class MedicalAgentState(TypedDict):
    # ... 原有字段
    retrieved_context: str   # RAG 检索到的医学知识上下文
```

#### 7. 启动初始化 (`main.py`)

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    # ... 原有初始化

    # RAG 初始化（异常不阻塞启动）
    try:
        vector_store = await init_vector_store(settings)
        retriever = MedicalRetriever(vector_store)
        disease_matcher.retriever = retriever
        advisor.retriever = retriever
    except Exception as e:
        print(f"Warning: RAG initialization failed: {e}")
```

### 诊疗指南文档

| 文件 | 覆盖内容 |
|------|----------|
| 感冒.md | 病因、临床表现、诊断要点、治疗原则、预防 |
| 高血压.md | 分级、危险因素、治疗目标、五大类降压药、生活方式干预 |
| 糖尿病.md | 分型、诊断标准、药物治疗（6类）、急性/慢性并发症 |
| 冠心病.md | 分型、典型症状、诊断方法、药物治疗、血运重建、急救 |
| 急性胃肠炎.md | 病因、脱水评估、治疗原则（补液是关键） |
| 支气管哮喘.md | 分级、控制药物、缓解药物、急性发作处理 |
| 颈椎病.md | 分型、诊断、保守治疗vs手术、预防 |
| 贫血.md | 分类、常见类型（缺铁/巨幼/再障）、饮食建议 |
| 焦虑症.md | 分类、核心症状、躯体症状、心理治疗vs药物治疗 |

### 容错设计

| 场景 | 处理 |
|------|------|
| 向量库初始化失败 | 打印警告，retriever=None，纯 LLM 模式运行 |
| 嵌入 API 调用失败 | 向量检索 try/except 静默跳过，结构化匹配仍可用 |
| 文档目录不存在 | `_load_guidelines()` 返回空列表，创建空集合 |
| 持久化目录已有数据 | 跳过重新加载，直接使用已有集合 |
| DashScope 批量限制 | `BATCH_SIZE=10` 分批添加 |

### 配置项

| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `EMBEDDING_MODEL` | `text-embedding-v3` | DashScope 嵌入模型 |
| `EMBEDDING_BASE_URL` | `https://dashscope.aliyuncs.com/compatible-mode/v1` | 嵌入 API 端点 |
| `CHROMA_PERSIST_DIR` | `./chroma_db` | Chroma 持久化目录 |
| `DATA_DIR` | `./data` | 诊疗指南文档目录 |

###  bug修复

  Bug 详情:backend/app/main.py:24 把 Chroma 实例直接传给 MedicalRetriever,但 retriever 内部调用
  self.vector_store.ainvoke(query)——Chroma 实例没有这个方法,必须 .as_retriever() 才有。异常被 try/except: pass
  完全吞掉,所有诊断 / 建议请求的【医学文献参考】块都从未生效,只有结构化知识库一路在工作。

  修复:
  - backend/app/main.py:24 改为 MedicalRetriever(vector_store.as_retriever(search_kwargs={"k": 3}))
  - backend/app/rag/retriever.py 两处 except Exception: pass 改为打印日志,避免下次类似问题再被静默掩盖

  修复后验证:retrieve_for_diagnosis 同时返回【知识库匹配结果】+【医学文献参考】;retrieve_for_advice
  同时返回【疾病详情】+【相关文献】片段。