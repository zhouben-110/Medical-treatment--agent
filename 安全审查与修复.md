# 医疗 Agent 安全审查与修复报告

**审查日期：** 2026-06-17
**审查范围：** 全栈（FastAPI 后端 + Next.js 前端 + MCP Server + PostgreSQL + Redis）
**审查人：** Claude Code

---

## 一、审查概览

| 级别 | 数量 | 状态 |
|------|------|------|
| 🔴 高危 | 3 | ✅ 已修复 |
| 🟠 中危 | 5 | ⏳ 待修复 |
| 🟡 低危 | 4 | ⏳ 待修复 |

---

## 二、高危问题（已修复）

### ✅ HIGH-1：API Key 认证可被绕过

**文件：** `backend/app/auth.py:16`
**CVSS 评分：** 8.1（高）

**问题描述：**
当 `API_KEY` 环境变量未设置时，`verify_api_key` 函数直接返回 `True`，所有 `/api/*` 端点完全无认证。生产环境若遗漏配置，等于裸奔。

**原代码：**
```python
if not settings.api_key:
    return True  # ← 认证完全跳过
```

**修复方案：**
未配置 `API_KEY` 时拒绝所有请求并返回 500 错误，同时记录错误日志。

**修复后代码：**
```python
if not settings.api_key:
    logger.error("API_KEY 未配置，拒绝所有请求。请在 .env 中设置 API_KEY。")
    raise HTTPException(
        status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
        detail="服务未正确配置：缺少 API_KEY",
    )
```

**影响：** 消除未授权访问风险

---

### ✅ HIGH-2：前端 API Key 暴露在浏览器中

**文件：** `frontend/.env.local`
**CVSS 评分：** 7.5（高）

**问题描述：**
`NEXT_PUBLIC_API_KEY=medical-agent-dev-key-2024` 使用 `NEXT_PUBLIC_` 前缀，Next.js 会将其打包进客户端 JS，任何人打开 DevTools 即可看到。

**修复方案：**

1. **创建 Next.js middleware**（`frontend/src/middleware.ts`）：
   - 在服务端拦截 `/api/*` 请求
   - 从服务端环境变量读取 `API_KEY`
   - 注入 `X-API-Key` 请求头

2. **更新前端客户端**（`frontend/src/api/client.ts`）：
   - 移除客户端 API Key 发送逻辑
   - `getHeaders()` 不再包含 `X-API-Key`

3. **更新环境变量**（`frontend/.env.local`）：
   - 移除 `NEXT_PUBLIC_` 前缀
   - 改为 `API_KEY=medical-agent-dev-key-2024`（仅服务端可见）

**新增文件：**
```typescript
// frontend/src/middleware.ts
export function middleware(request: NextRequest) {
  const apiKey = process.env.API_KEY;
  if (request.nextUrl.pathname.startsWith('/api/') && apiKey) {
    const requestHeaders = new Headers(request.headers);
    requestHeaders.set('X-API-Key', apiKey);
    return NextResponse.next({ request: { headers: requestHeaders } });
  }
  return NextResponse.next();
}
```

**影响：** API Key 不再暴露到浏览器，密钥仅存在于服务端

---

### ✅ HIGH-3：LangSmith 追踪泄露患者医疗数据

**文件：** `backend/.env:5`
**CVSS 评分：** 7.2（高）

**问题描述：**
`LANGCHAIN_TRACING_V2=true` 开启后，所有 LLM 交互（包含患者症状描述、诊断结果、对话历史）都会发送到 LangSmith 云端服务。对于医疗应用，这构成严重的隐私数据外泄。

**修复方案：**
- 将 `LANGCHAIN_TRACING_V2` 默认值改为 `false`
- 添加注释说明：如需调试，应使用自托管 LangSmith
- 同步更新 `.env.example` 的默认值和注释

**修复后：**
```bash
# LangSmith 追踪（默认关闭，防止患者医疗数据外泄到云端）
# 如需调试，临时开启并确保使用自托管 LangSmith
LANGCHAIN_TRACING_V2=false
```

**影响：** 患者医疗数据不再外泄到第三方云服务

---

## 三、中危问题（待修复）

### ⏳ MED-1：无 Prompt Injection 鲁棒性

**文件：** `backend/app/nodes/*.py`（多个节点）

**问题描述：**
所有用户消息直接拼接进 LLM prompt，无任何净化或防护：
- `triage.py:62` — `用户描述: {user_message}`
- `symptom_analyzer.py:18` — `用户描述: {user_message}`
- `questioner.py:12` — `对话历史: {conversation_history}`
- `diagnose_and_advise.py:17` — `对话: {conversation_history}`

用户可通过构造输入劫持 LLM 行为。

**建议修复：**
- 添加输入净化层，过滤常见注入模式
- 使用 LangChain 的 `PromptGuard` 或自定义 guardrail
- 在 system prompt 中强化角色边界

---

### ⏳ MED-2：CORS 配置过于宽松

**文件：** `backend/app/main.py:60-66`

**问题描述：**
```python
allow_credentials=True,
allow_methods=["*"],
allow_headers=["*"],
```

**建议修复：**
限制为实际使用的 HTTP 方法和必要的 Header：
```python
allow_methods=["GET", "POST", "DELETE"],
allow_headers=["Content-Type", "X-API-Key"],
```

---

### ⏳ MED-3：限流 Fail-Open 设计

**文件：** `backend/app/redis.py:90-91, 103-104`

**问题描述：**
Redis 故障时限流完全失效（`return True`），API 可被无限调用。

**建议修复：**
降级为内存限流（如 `slowapi`），而非完全放行。

---

### ⏳ MED-4：Health 端点泄露内部状态

**文件：** `backend/app/main.py:78-129`

**问题描述：**
`/health` 无需认证，暴露数据库连接池、Redis 状态、活跃会话数等内部信息。

**建议修复：**
简化为 `{"status": "ok"}`，详细信息移到认证后的管理员接口。

---

### ⏳ MED-5：无用户身份认证体系

**文件：** `backend/app/models.py`

**问题描述：**
`users` 表仅有 `id` 和 `created_at`，无用户名/密码/邮箱字段。会话的 `user_id` 可空且从未被填充。

**建议修复：**
实现 JWT/Session 认证，将会话绑定到真实用户。

---

## 四、低危问题（待修复）

### ⏳ LOW-1：Alembic 配置文件硬编码数据库凭据

**文件：** `backend/alembic.ini:89`

**问题描述：**
```ini
sqlalchemy.url = postgresql+asyncpg://postgres:postgres@localhost:5432/medical_agent
```

**建议修复：**
改为 `sqlalchemy.url =` 空值，依赖 `env.py` 从环境变量读取。

---

### ⏳ LOW-2：服务监听所有网络接口

**文件：** `backend/start.py:12`

**问题描述：**
```python
uvicorn.run("app.main:app", host="0.0.0.0", port=8000, reload=True)
```

**建议修复：**
生产环境使用 `host="127.0.0.1"` 并关闭 `reload`。

---

### ⏳ LOW-3：内存缓存无上限保护

**文件：** `backend/app/cache.py:34-36`

**问题描述：**
淘汰策略为一次性删除最旧的一半，存在内存抖动风险。

**建议修复：**
使用 `functools.lru_cache` 或直接依赖 Redis 统一缓存。

---

### ⏳ LOW-4：前端消息渲染未做 XSS 过滤

**文件：** `frontend/src/components/MessageBubble.tsx:40`

**问题描述：**
```tsx
<p className="whitespace-pre-wrap">{message.content}</p>
```

当前 React 默认转义 JSX 文本，是安全的。但如果未来改用 `dangerouslySetInnerHTML` 渲染 Markdown，会产生 XSS 风险。

**建议修复：**
如需 Markdown 渲染，使用 `react-markdown` + `rehype-sanitize`。

---

## 五、安全亮点 ✅

项目中已有的良好安全实践：

| 实践 | 位置 | 说明 |
|------|------|------|
| 时序安全比较 | `auth.py:19` | 使用 `hmac.compare_digest` 防止时序攻击 |
| 输入长度限制 | `schemas.py:8` | `max_length=2000` 防止超长输入 |
| 环境变量隔离 | `.gitignore` | `.env` 已排除，未被 git 追踪 |
| 占位符模板 | `.env.example` | 使用占位符而非真实凭据 |
| MCP 只读标记 | `server.py:16-21` | `readOnlyHint=True` 标记只读工具 |
| 优雅降级 | `redis.py` | Redis 不可用时服务不崩溃 |
| 结构化输出 | `symptom_analyzer.py` | Pydantic 验证 LLM 输出 |

---

## 六、修复文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `backend/app/auth.py` | 修改 | 未配置 API_KEY 时拒绝请求 |
| `frontend/src/middleware.ts` | 新增 | 服务端注入 API Key |
| `frontend/src/api/client.ts` | 修改 | 移除客户端 API Key 发送 |
| `frontend/.env.local` | 修改 | 移除 `NEXT_PUBLIC_` 前缀 |
| `frontend/.env.example` | 新增 | 前端环境变量模板 |
| `backend/.env` | 修改 | 关闭 LangSmith 追踪 |
| `backend/.env.example` | 修改 | 添加追踪关闭注释 |

---

## 七、后续建议

1. **优先修复中危问题**，特别是 Prompt Injection 防护和 CORS 收紧
2. **建立安全基线**：在 CI/CD 中添加安全扫描（如 `bandit`、`safety`）
3. **定期轮换密钥**：DashScope、LangSmith、数据库密码
4. **添加审计日志**：记录所有 API 调用的来源 IP、时间、操作
5. **医疗合规审查**：如面向真实患者使用，需符合相关医疗数据法规
