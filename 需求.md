搭建一个简单的医疗 agent，可以根据症状描述给出治疗方案

## 工作流程

用户描述症状 → 症状分析 → 追问决策 → 疾病匹配 → 治疗建议

LangGraph 图结构：

```
START → analyze ──(need_more_info && turns<5)──> question → END
              └──(否则)──────────────────────> diagnose → advise → END
```

- **analyze**：从用户最新消息提取症状，与已有症状去重合并，判定是否还需要更多信息
- **question**：症状不足时生成 1-3 个追问（持续时间、伴随症状、既往病史、过敏史）
- **diagnose**：基于症状组合匹配至多 3 个可能疾病，并给出置信度
- **advise**：生成治疗建议（用药、生活调理、是否就医），末尾自动追加免责声明
- **MAX_USER_TURNS = 5**：用户回合数达到上限后强制进入诊断分支，避免无限追问

## 技术栈

### 后端

- **Python 3.10+**
- **FastAPI** — Web 框架，提供 REST API
- **LangChain** — LLM 编排框架，管理 prompt 模板与链式调用
- **LangGraph** — 有状态多轮对话图引擎，驱动症状分析→追问→诊断→建议的完整流程
- **langgraph-checkpoint-sqlite（AsyncSqliteSaver）** — 基于 SQLite 的异步 checkpoint 持久化，支持跨请求的多轮对话状态恢复（独立文件 `medical_agent_checkpoints.db`）
- **SQLAlchemy 2.x + aiosqlite** — 异步 ORM，管理会话、消息、症状库等业务表（`medical_agent.db`）
- **阿里千问（qwen-plus）** — 大语言模型，通过 `langchain-openai` 的 `ChatOpenAI` 接入千问 OpenAI 兼容端点（base_url：`https://dashscope.aliyuncs.com/compatible-mode/v1`）
- **Pydantic v2 + pydantic-settings** — 请求/响应数据校验与 `.env` 配置加载
- **SSE（Server-Sent Events）** — 流式输出协议，前端实时接收 LLM 生成的 token
- **pytest + pytest-asyncio + httpx** — 异步接口测试

### 前端

- **Next.js 14（App Router）** — React 框架
- **TypeScript** — 类型安全
- **Tailwind CSS** — 样式方案
- **Fetch API + ReadableStream** — SSE 流式消费（非 EventSource，便于用 POST 提交 JSON）
- **Next.js rewrites** — `/api/*` 反向代理到 `http://localhost:8000/api/*`，规避跨域

## 关键设计

- **LangGraph checkpoint 多轮对话**：每次用户消息触发一次完整图执行，状态通过 `AsyncSqliteSaver` 按 `thread_id = session_id` 持久化；下一次消息基于 checkpoint 续跑，症状与诊断信息自动累积
- **astream_events 流式过滤**：使用 `astream_events(version="v2")` 按 `metadata.langgraph_node` 过滤，仅将 `question` 和 `advise` 节点的 LLM token 推送给前端；`analyze` 和 `diagnose` 不流式（结构化解析节点，输出不直接给用户看）
- **双存储分离**：
  - SQLAlchemy `messages` 表 — 服务 UI 历史回放
  - LangGraph checkpointer — 服务图状态续跑
  - 两条路径互不干扰，可独立清理
- **流式与非流式双接口**：`POST /api/chat` 一次性返回完整 JSON，`POST /api/chat/stream` 返回 SSE；前端默认走流式

## 数据模型

业务库（`medical_agent.db`）：

| 表 | 主要字段 | 说明 |
|---|---|---|
| `users` | id, created_at | 预留用户表（当前未启用鉴权） |
| `sessions` | id, user_id, title, created_at, diagnosis | 一次会话；`diagnosis` 在 advise 节点完成后回填首位疾病 |
| `messages` | id, session_id, role, content, timestamp | 用户/助手消息流水，供历史回放 |
| `symptom_categories` | id, name | 症状分类（头部/胸部/腹部/四肢/全身/皮肤） |
| `symptoms` | id, category_id, name | 预置症状词表，由 `seed_data.py` 初始化 |

LangGraph 状态（`MedicalAgentState`）：

```
messages: Annotated[List, add_messages]   # LangGraph 自动累积
symptoms: List[str]                        # 已识别症状（去重合并）
current_stage: str                         # analyzing / questioning / diagnosing / completed
confidence: float                          # 0~1
possible_diseases: List[str]
treatment_plan: str
need_more_info: bool
session_id: str
```

## API 接口

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/chat` | 发送消息，一次性返回 `ChatResponse`（reply/stage/symptoms/possible_diseases/need_more_info/session_id） |
| POST | `/api/chat/stream` | 同上，但以 SSE 流返回 |
| GET  | `/api/history` | 会话列表（id/title/created_at/message_count） |
| GET  | `/api/history/{session_id}` | 单个会话详情（messages + diagnosis） |
| GET  | `/api/symptoms` | 预置症状库（按分类聚合） |

SSE 事件协议：

| `type` | 字段 | 说明 |
|---|---|---|
| `meta` | session_id / symptoms / stage / need_more_info / possible_diseases | 首帧只含 session_id；末帧含全量元数据 |
| `stage` | stage | 阶段切换通知（questioning / completed） |
| `chunk` | content / stage | LLM token 片段 |
| `done` | — | 流结束 |
| `error` | content | 异常信息 |

## 前端结构

- `app/page.tsx` → `<ChatWindow />` 单页入口
- `components/ChatWindow.tsx` — 消息收发、流式拼接、阶段标签注入
- `components/Sidebar.tsx` — 历史会话列表 + 新建会话
- `components/MessageBubble.tsx` — 消息气泡，按 `stage` 渲染"追问 / 诊断与建议"标签
- `components/SymptomTags.tsx` — 已识别症状的徽章展示
- `api/client.ts` — 封装 fetch + SSE 解析（`sendMessage` / `sendMessageStream` / `getHistory` / `getSessionDetail` / `getSymptoms`）
- `types/index.ts` — `ChatStage` / `Message` / `Session` / `ChatResponse` / `StreamMeta` / `SymptomCategory`

## 功能需求

- 流式输出（SSE）
- 多轮追问（症状不足时自动追问，最多 5 轮用户输入）
- 会话管理（新建 / 切换 / 历史回放）
- 症状标签展示（已识别症状的徽章）
- 阶段标识（气泡上区分"追问"与"诊断与建议"）
- 预置症状词表（6 分类、30+ 常见症状）

## 非功能与约束

- **中文优先**：所有 prompt、UI 文案、症状词表均为中文
- **免责声明**：advise 节点输出末尾强制追加 "以上内容仅供参考，不构成医疗建议。如有不适，请及时就医。"
- **CORS**：仅放行 `http://localhost:3000 / 3001 / 3002`
- **配置**：`.env` 提供 `llm_api_key`（DASHSCOPE API Key）、`llm_model`、`llm_base_url`、`database_url`，缺省走千问兼容端点
- **数据隐私**：本地 SQLite 持久化，不外传；无内置鉴权，仅适用于本地/单机演示
- **错误处理**：SSE 通道异常会以 `type:error` 事件回传前端，由 ChatWindow 在气泡内显示失败原因
- **温度策略**：analyze / diagnose 使用 `temperature=0`（结构化抽取要稳定），question / advise 使用 `temperature=0.3`（生成式回答略带变化）

## RAG 医学知识检索增强

### 背景
初版系统疾病匹配和治疗建议完全依赖 LLM 参数记忆，缺乏医学知识库支撑。为提升诊断准确性和减少幻觉，接入 RAG（Retrieval-Augmented Generation）。

### 方案
采用**结构化数据 + 向量检索**双通道架构：
- **结构化知识库**（`knowledge_base.py`）：25 种常见疾病的症状映射、治疗建议、就医指征，基于症状交集做精确匹配
- **向量检索**（Chroma + DashScope text-embedding-v3）：加载 `data/guidelines/*.md` 诊疗指南文档，按语义相似度检索补充

### 架构
```
用户输入 → analyze → question 循环 → 症状列表
                                        ↓
                            MedicalRetriever.retrieve_for_diagnosis()
                            ├─ 结构化匹配（症状交集，按匹配度排序）
                            └─ 向量检索（Chroma + DashScope 嵌入）
                                        ↓
                            diagnose 节点（LLM + 检索结果注入 prompt）
                                        ↓
                            MedicalRetriever.retrieve_for_advice()
                            ├─ 结构化查详情（疾病名精确查找）
                            └─ 向量检索（诊疗指南文档）
                                        ↓
                            advise 节点（LLM + 检索结果注入 prompt）
```

### 新增文件
| 文件 | 说明 |
|------|------|
| `backend/app/rag/__init__.py` | RAG 模块入口 |
| `backend/app/rag/knowledge_base.py` | 结构化疾病-症状知识库（25 种疾病） |
| `backend/app/rag/vector_store.py` | Chroma 向量库初始化与文档加载 |
| `backend/app/rag/embeddings.py` | DashScope 嵌入模型封装（处理批量限制） |
| `backend/app/rag/retriever.py` | 统一检索接口（结构化 + 向量） |
| `backend/data/guidelines/*.md` | 医学诊疗指南文档（9 份） |

### 修改文件
| 文件 | 改动 |
|------|------|
| `backend/app/config.py` | 新增 `embedding_model`、`embedding_base_url`、`chroma_persist_dir`、`data_dir` |
| `backend/app/state.py` | 新增 `retrieved_context: str` 字段 |
| `backend/app/nodes/disease_matcher.py` | 注入 retriever，prompt 加入 `{medical_context}` |
| `backend/app/nodes/advisor.py` | 注入 retriever，prompt 加入 `{medical_context}` |
| `backend/app/main.py` | lifespan 中初始化向量库并注入 retriever |
| `backend/requirements.txt` | 新增 `chromadb`、`langchain-chroma` |

### 关键设计决策
- **DashScope 批量限制**：text-embedding-v3 单次最多处理 10 条文本，`vector_store.py` 按 `BATCH_SIZE=10` 分批添加
- **自定义嵌入类**：DashScope OpenAI 兼容端点格式差异，使用 `dashscope.TextEmbedding` 直接调用
- **优雅降级**：RAG 初始化失败时打印警告，retriever 设为 None，主流程不受影响
- **持久化检查**：Chroma 持久化目录存在且集合非空时跳过重新加载，避免重复嵌入计算

### 知识库内容
- **结构化数据**：25 种常见疾病（感冒、流感、支气管炎、肺炎、哮喘、高血压、冠心病、心律失常、胃肠炎、胃溃疡、胆囊炎、泌尿感染、糖尿病、甲亢、贫血、颈椎病、腰椎间盘突出、关节炎、焦虑症、抑郁症、荨麻疹、湿疹、过敏性鼻炎、偏头痛、中暑）
- **诊疗指南**：9 份 Markdown 文档（感冒、高血压、糖尿病、冠心病、急性胃肠炎、支气管哮喘、颈椎病、贫血、焦虑症）
