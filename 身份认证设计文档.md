# 身份认证实现总结

## 实现概述

已完成基于 Supabase Auth 的身份认证体系实现，支持用户注册、登录、JWT Token 认证等功能。

## 已完成的工作

### 后端 (Backend)

1. **配置更新**
   - 添加 Supabase 配置项到 `config.py`：`supabase_url`、`supabase_anon_key`、`supabase_jwt_secret`
   - 更新 `.env.example` 添加 Supabase 配置说明

2. **User 模型扩展**
   - 添加 `email` 字段（唯一索引）
   - 添加 `is_active` 字段
   - User ID 现在使用 Supabase 的 user id

3. **认证模块重构** (`auth.py`)
   - 实现 `decode_supabase_token()` 函数解码 Supabase JWT
   - 实现 `get_current_user()` 依赖获取当前认证用户
   - 实现 `get_optional_user()` 依赖支持可选认证
   - 保留 `verify_api_key()` 向后兼容

4. **路由更新**
   - `chat.py` - 使用用户认证，Session 自动绑定用户
   - `history.py` - 只返回当前用户的 Session，验证访问权限
   - `symptoms.py` - 移除认证要求（公开访问）
   - `auth.py` - 新增 `/api/auth/me` 端点获取用户信息

5. **数据库迁移**
   - 创建迁移脚本添加 `email` 和 `is_active` 字段

6. **依赖更新**
   - 添加 `python-jose[cryptography]` 用于 JWT 验证

### 前端 (Frontend)

1. **Supabase SDK 集成**
   - 安装 `@supabase/supabase-js` 和 `@supabase/ssr`
   - 创建 `lib/supabase.ts` 配置 Supabase 客户端

2. **认证上下文** (`contexts/AuthContext.tsx`)
   - 管理用户状态和 session
   - 提供 `signIn`、`signUp`、`signOut` 方法
   - 监听认证状态变化

3. **登录/注册页面**
   - `app/login/page.tsx` - 登录页面
   - `app/register/page.tsx` - 注册页面（支持邮箱验证提示）

4. **API 客户端更新** (`api/client.ts`)
   - 自动获取 Supabase access token
   - 添加 Authorization header
   - 处理 401 错误

5. **用户导航组件** (`components/UserNav.tsx`)
   - 显示用户邮箱
   - 登出按钮
   - 未登录时显示登录/注册链接

6. **布局更新** (`app/layout.tsx`)
   - 集成 AuthProvider
   - 添加顶部导航栏

7. **路由保护** (`middleware.ts`)
   - 未登录用户重定向到登录页
   - 已登录用户访问登录/注册页重定向到首页
   - 保留 API Key 注入（向后兼容）

8. **环境变量配置**
   - 更新 `.env.example` 添加 Supabase 配置

### 文档

1. `SUPABASE_AUTH_SETUP.md` - Supabase 配置指南
2. `AUTH_IMPLEMENTATION_SUMMARY.md` - 本文档

## 使用流程

### 1. 配置 Supabase

1. 在 [supabase.com](https://supabase.com) 创建项目
2. 获取 Project URL、Anon Key、JWT Secret
3. 配置环境变量

### 2. 运行数据库迁移

```bash
cd backend
python -m alembic upgrade head
```

### 3. 启动应用

```bash
# 后端
cd backend
python start.py

# 前端
cd frontend
npm run dev
```

### 4. 注册和登录

1. 访问 `http://localhost:3000/register` 注册
2. 检查邮箱进行验证（如果启用）
3. 访问 `http://localhost:3000/login` 登录
4. 开始使用医疗助手

## 技术架构

### 认证流程

```
用户 -> 前端 (Supabase SDK) -> Supabase Auth
                                    ↓
                              返回 JWT Token
                                    ↓
用户 -> 前端 (API Client) -> 后端 (验证 JWT) -> 数据库
```

### 关键组件

- **Supabase Auth** - 用户管理、JWT 签发
- **Supabase SDK** - 前端认证状态管理
- **python-jose** - 后端 JWT 验证
- **FastAPI Dependencies** - 认证中间件

## 安全特性

1. **JWT Token** - 无状态认证，自动过期
2. **邮箱验证** - 可选，防止虚假注册
3. **密码加密** - Supabase 处理，使用 bcrypt
4. **权限隔离** - 用户只能访问自己的 Session
5. **Rate Limiting** - 防止暴力破解
6. **CORS 配置** - 限制允许的源

## 向后兼容

1. **API Key 认证** - 保留 `verify_api_key()` 函数
2. **公开端点** - `/api/symptoms` 无需认证
3. **可选认证** - `get_optional_user()` 支持可选认证

## 后续扩展

1. **OAuth 登录** - 集成 Google、GitHub 等第三方登录
2. **密码重置** - 实现忘记密码功能
3. **用户资料** - 扩展用户信息管理
4. **角色权限** - 实现管理员、医生等角色
5. **会话管理** - 多设备登录管理

## 故障排除

### Token 验证失败

检查后端 `.env` 中的 `SUPABASE_JWT_SECRET` 是否正确。

### 前端无法登录

检查 `frontend/.env.local` 中的 Supabase 配置。

### 数据库迁移失败

确保数据库连接正常，然后运行：
```bash
python -m alembic upgrade head
```

### CORS 错误

检查后端 `.env` 中的 `CORS_ORIGINS` 配置。



### bug审查与修复
问题有两个：

  1. 数据库表不存在

  后端模型代码定义了 users、sessions、messages 等表，但 PostgreSQL
  数据库中实际没有这些表。当后端尝试查询时直接抛异常，返回 500。

  → 执行 init_db() 创建了所有表解决。

  2. Supabase JWT 算法变更（核心问题）

  Supabase 近期将用户登录后的 JWT 签名算法从 HS256（对称密钥）切换到了
  ES256（椭圆曲线非对称密钥）。

  后端原代码 auth.py 中硬编码了 algorithms=["HS256"]，所以所有带 ES256 Token
  的请求都被拒绝，返回 401。

  修复方式：
  - 根据 Token 头部的 alg 字段自动判断算法
  - HS256 → 用 SUPABASE_JWT_SECRET 验证
  - ES256 → 从 Supabase 的 JWKS 端点 (/auth/v1/.well-known/jwks.json) 获取公钥验证