# Changelog

## 2026-06-03 - 全面代码质量优化 (P0-P3)

### 变更概述

基于工作流分析，对项目进行 16 项系统性优化，覆盖架构规范、性能、安全、可维护性四个维度。

### P0 - 立即修复

**1. models.py / schemas.py 命名修正**
- `models.py` 存放 ORM 模型（SQLAlchemy），`schemas.py` 存放 Pydantic 模型（请求/响应）
- 原先命名完全颠倒，已交换内容并更新所有导入（history.py、symptoms.py、chat.py、seed_data.py、alembic/env.py）

**2. vector_store.py 异步阻塞修复**
- `init_vector_store` 改用 `asyncio.to_thread()` 包装同步调用
- 修复启动期间阻塞事件循环的问题

**3. embed_documents 自动分批**
- `embed_documents` 内部实现自动分批逻辑（batch_size=10）
- DashScope API 限制单次最多 10 条，之前仅在 vector_store.py 写入时分批

**4. get_history N+1 查询修复**
- 使用 `outerjoin + group_by + func.count` 单次查询替代循环查询
- 100 个会话从 101 次查询降为 1 次

### P1 - 短期优化

**5. LLM 实例统一管理**
- 新增 `app/llm.py` 工厂模块，`get_llm(temperature)` 带缓存
- 四个节点文件（symptom_analyzer、questioner、disease_matcher、advisor）共享实例

**6. disease_matcher 结构化输出**
- LLM 输出改为 JSON 格式，使用 Pydantic `DiseaseDiagnosis` 模型验证
- 替代原先脆弱的正则表达式解析，保留 fallback 兼容

**7. /health 健康检查端点**
- 新增 `GET /health`，检查数据库连接、连接池状态、LangGraph 状态、缓存条目数

**8. 前端消息 key 改用唯一 ID**
- `Message` 接口新增 `id: string` 字段，使用 `crypto.randomUUID()` 生成
- 替代原先的数组 index 作为 React key

**9. analyze/diagnose 进度反馈**
- SSE 新增 `PROGRESS_NODES` 映射，`on_chain_start` 事件触发时推送 `stage` 事件
- 前端可展示"分析中"/"诊断中"进度提示

**10. 前端 SSE 错误日志**
- client.ts 中 catch 块从静默忽略改为 `console.debug` 记录

### P2 - 中期改进

**11. 症状匹配算法升级**
- 从 `|A∩B|/|B|` 改为 Dice 系数 `2*|A∩B|/(|A|+|B|)`
- 消除短症状列表疾病天然占优的偏差

**12. ChatWindow 拆分**
- 新增 `hooks/useChat.ts`（消息收发、流式拼接、状态管理）
- 新增 `hooks/useSession.ts`（会话列表、加载、删除）
- ChatWindow.tsx 从 194 行缩减至 ~100 行

**13. 输入框改为 textarea**
- 支持 Shift+Enter 换行，Enter 发送
- 医疗场景下用户可详细描述症状

**14. API Key 时序攻击防护**
- `auth.py` 中 `==` 比较改为 `hmac.compare_digest()`

**15. session_id UUID 验证**
- `ChatRequest.session_id` 从正则字符串改为 Pydantic `UUID` 类型
- 自动验证 UUID 结构，接受大小写

### P3 - 长期演进

**16. 向量检索合并查询**
- `retrieve_for_advice` 将多个疾病的向量查询合并为单次调用
- 减少网络往返和嵌入计算

**17. 对话摘要机制**
- 新增 `app/summarizer.py`，消息超过 12 条时自动摘要早期对话
- 保留最近 4 条原文 + LLM 生成的摘要（200 字以内）
- 避免长对话 token 超限

**18. 诊断结果缓存**
- 新增 `app/cache.py`，相同症状组合 1 小时内复用诊断结果
- 内存缓存，上限 200 条，LRU 淘汰

**19. 连接池监控**
- `/health` 端点新增 pool 状态（size/checked_in/checked_out/overflow）
- 新增缓存条目数监控

### 新增文件

| 文件 | 说明 |
|------|------|
| `backend/app/llm.py` | LLM 实例工厂（带缓存） |
| `backend/app/summarizer.py` | 对话摘要机制 |
| `backend/app/cache.py` | 诊断结果缓存 |
| `frontend/src/hooks/useChat.ts` | 聊天状态 Hook |
| `frontend/src/hooks/useSession.ts` | 会话管理 Hook |

### 修改文件

| 文件 | 改动 |
|------|------|
| `backend/app/models.py` | 内容交换为 ORM 模型 |
| `backend/app/schemas.py` | 内容交换为 Pydantic 模型，session_id 改用 UUID |
| `backend/app/routers/chat.py` | 导入修正、摘要集成、进度事件 |
| `backend/app/routers/history.py` | 导入修正、N+1 查询修复 |
| `backend/app/routers/symptoms.py` | 导入修正 |
| `backend/app/nodes/symptom_analyzer.py` | 使用 llm.py 工厂 |
| `backend/app/nodes/questioner.py` | 使用 llm.py 工厂 |
| `backend/app/nodes/disease_matcher.py` | 结构化输出 + 缓存 + llm.py 工厂 |
| `backend/app/nodes/advisor.py` | 使用 llm.py 工厂 |
| `backend/app/rag/vector_store.py` | asyncio.to_thread 修复 |
| `backend/app/rag/embeddings.py` | 自动分批逻辑 |
| `backend/app/rag/retriever.py` | 向量检索合并查询 |
| `backend/app/rag/knowledge_base.py` | Dice 系数匹配算法 |
| `backend/app/auth.py` | hmac.compare_digest |
| `backend/app/main.py` | /health 端点增强 |
| `backend/seed_data.py` | 导入修正 |
| `backend/alembic/env.py` | 导入修正 |
| `frontend/src/types/index.ts` | Message 新增 id 字段 |
| `frontend/src/components/ChatWindow.tsx` | 拆分为 Hook + textarea |
| `frontend/src/api/client.ts` | SSE 错误日志 |

---

## 2026-05-31 - 数据库架构迁移

### 变更概述

将项目从 SQLite + ChromaDB 迁移至 PostgreSQL 统一数据层。

### 具体变更

**1. 应用数据库：SQLite → PostgreSQL**
- `aiosqlite` 替换为 `asyncpg`
- 数据库 URL 从 `sqlite+aiosqlite:///./medical_agent.db` 改为 `postgresql+asyncpg://...`
- 引入 Alembic 管理数据库迁移

**2. LangGraph Checkpoint：SQLite → PostgreSQL**
- `langgraph-checkpoint-sqlite` 替换为 `langgraph-checkpoint-postgres`
- 对话状态持久化从 `medical_agent_checkpoints.db` 迁移到 PostgreSQL
- 启动时自动创建 checkpoint 表（`saver.setup()`）

**3. 向量存储：ChromaDB → pgvector**
- `chromadb` + `langchain-chroma` 替换为 `langchain-postgres` + `pgvector`
- 向量数据从本地文件 `./chroma_db` 迁移到 PostgreSQL
- embedding 维度：1024（DashScope text-embedding-v3）
- 保留分批处理逻辑（DashScope API 限制 batch_size <= 10）

**4. 前端方向确认**
- 保持 Next.js 14 + React 18（与原始技术栈声明不一致，已更新文档）

### 涉及文件

| 文件 | 变更 |
|---|---|
| `backend/requirements.txt` | 依赖更新 |
| `backend/app/config.py` | 数据库 URL、移除 chroma_persist_dir |
| `backend/app/main.py` | checkpoint 从 SQLite 改为 PostgreSQL |
| `backend/app/rag/vector_store.py` | 重写为 pgvector |
| `backend/.env` | 数据库连接字符串 |
| `backend/.env.example` | 数据库连接字符串 |
| `backend/alembic/` | 新增 Alembic 迁移配置 |
| `README.md` | 更新技术栈文档 |
